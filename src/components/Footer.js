import { version } from '../../package.json'

const Footer = () => {
    return (
        <div>
            <div>Footer</div>
            <div>Version: {version}</div>
        </div>
    )
}

export default Footer
