node_modules/
.DS_Store

# Environment variables
.env

# Arc environment values
# preferences.arc

# Arc esbuild
.build

# File built with 'arc deploy --dry-run'
sam.json

# File built with 'arc deploy' that gets uploaded to CloudFormation
sam.yaml

# Seeded database file for working locally with fake data
sandbox-seed.json

# Next JS
.next

# TS generated file from tsc --noEmit
tsconfig.tsbuildinfo

# Bundled directories
dist

# Parcel Cache
.parcel-cache