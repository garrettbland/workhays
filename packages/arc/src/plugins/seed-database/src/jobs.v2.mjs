/**
 * Jobs database seeds
 */

/**
 * ACCESS PATTERNS
 *
 * - Get active jobs, sort DESC by expires, limit to 10 results
 * - Get active job by category, sort DESC by expires, limit to 10 results
 * - Get active job
 * -- Could just be "Get job by id". Then if DB see's it's expired attribute, return 404
 * - Get active jobs by employer
 * - Get jobs by employer
 */
